<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.RecommendMapper">

	<update id="update1">
	
		UPDATE BOARD
		SET BOA_UP = NVL(BOA_UP,0) + 1
		WHERE BOA_NUM = #{boa_num}
	
	</update>
		
	<update id="update2">
	
		UPDATE BOARD
		SET BOA_DOWN = NVL(BOA_DOWN,0) + 1
		WHERE BOA_NUM = #{boa_num}
	
	</update>
	
	<resultMap type="HashMap" id="up_down">
	
		<result column="BOA_UP" property="boa_up"/>
		<result column="BOA_DOWN" property="boa_down"/>
	
	</resultMap>
	
	
	<select id="select" resultMap="up_down">
	
		SELECT BOA_UP, BOA_DOWN
		FROM BOARD
		WHERE BOA_NUM = #{boa_num}
	
	</select>

</mapper>